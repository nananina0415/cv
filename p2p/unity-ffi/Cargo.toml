[package]
name = "unity-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
p2p-core = { path = "../core" }
anyhow = "1"
tokio = { version = "1", features = ["rt"] }
